  //------- DEFAULT SETUP -----------
    // camera change setup
    if (autoCamera) {
      myCam.useCam();
    }
    else {
      beginCamera();
      perspective();
      camera();
      endCamera();
    }
  smooth();
  background(0);
  translate(width/2, height/2);
  pushMatrix();
  setupAxis();


